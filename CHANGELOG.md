# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

## [0.5.0] - 2026-05-28

### Added
- **Nodemaidノードエディタモード** — Blender風カード型ノードベースエディタ
  - モード切替トグル（スライディングピルUI、Flowmaid ⇄ Nodemaid）
  - カード型ノード（Generic / Service / Table の3種）
  - テーブルノード: カラム直接編集、PK/FK/NN/UQ制約トグル、論理名/物理名切替
  - テーブル詳細編集モーダル（D&Dカラム並替、チェックボックス制約）
  - ポート間エッジ接続（ホバーハンドル、接続済み常時表示）
  - エッジ右クリックでカーディナリティ設定（1:1, 1:N, N:M, 0:1, 0:N）
  - カーディナリティラベル表示（カスタムエッジ）
  - フォーマットバー（塗りつぶし色・枠線色・文字色・枠線スタイル、ピン固定）
  - サブモード切替（汎用 / API連携 / ER図）
  - Ctrl+C/V コピー&ペースト、Ctrl+Z/Shift+Z undo/redo、Ctrl+A 全選択
  - .nodeeditor エクスポート/インポート
  - zustand+zundo独立ストア、localStorage自動保存
  - i18n対応（日本語/英語）
  - Mermaid erDiagram出力（テーブルノード対応）
- F2キーで選択中ノード/エッジのラベル編集を開始 (#49)
- ツールバーに両サイドパネルの一括開閉ボタン (#48)

### Fixed
- Mermaid Importのプレースホルダーを `flowchart TD` に更新 (#25)
- 初回ノード配置時にキャンバスのズームが変わる問題を修正

### Changed
- `/url` スキルをポート3000固定 + 既存プロセスクリーンアップに改善

## [0.4.4] - 2026-04-13

### Added
- ホイール操作の統一: ホイール→上下パン、Shift+ホイール→左右パン、Ctrl/Cmd+ホイール→ズーム (#37)
- ズーム範囲を30%〜400%に拡大（デフォルト50%〜200%→大きなフローでも全体表示可能に） (#37)
- 全キャンバス（メイン/一括編集/差分比較）でホイール操作・ズーム範囲を共通化

## [0.4.3] - 2026-04-11

### Added
- ノード作成時のブルブルアニメーション（rotate 0.25s）
- ノード削除時のblur+フェードアウトアニメーション（0.25s、子ノード再帰対応）
- エッジ接続時のゴールド星型パーティクルエフェクト（ターゲット位置に8個バースト）
- 選択ノードの右上に✕削除ボタン（全ノードタイプ統一、ズーム補正対応）
- Deleteキーでのアニメーション付き削除（React Flow内蔵→自前ハンドラ）
- ドロップ配置後のアニメーション完了→フォーカス遅延

### Fixed
- コンポーネント配置時にフォーマットバーが消える問題（親ノードIDを正しく選択）
- NodeLabelのネイティブドラッグ防止（draggable=false）

## [0.4.2] - 2026-04-11

### Added
- ゴーストノードのサイズを元ノードから引き継ぎ (#15)
  - 同じ形状の候補はソースノードの実サイズを使用、異なる形状はデフォルトサイズ
- ゴーストノード表示/非表示トグルボタン（ツールバー、localStorage永続化）
- ゴーストノードのフェードインアニメーション（blur + グロー、0.25s）
- ゴーストクリックでノード作成後、新ノードにフォーカス（選択状態に）
- Shift+リサイズ時の斜めオレンジ点線インジケーター（比率維持の視覚フィードバック）

### Changed
- ノード移動中はゴーストノードを非表示に（ドラッグ操作の邪魔を防止）

### Fixed
- ゴーストノードのエッジ接続位置がずれる問題（node.width/heightを直接設定）

## [0.4.1] - 2026-04-08

### Fixed
- 等間隔スナップでドラッグ中のノードがスナップ位置からずれて見える問題 (#42)
  - `onNodeDrag` で store の最新状態を参照し、applySnap 適用後の位置でガイドを描画
- リサイズ時のスナップで `getNodeBounds` / `applySnap` が `measured` 優先だったため、`node.width/height` と乖離した値で誤判定する問題
  - `node.width` / `node.height` を最優先参照に変更
- リサイズ時にマウス移動で gauge がチカチカする問題
  - alignment snap を grid 丸めより先に適用し、snap しない場合のみ grid にフォールバック

### Changed
- リサイズ時のスナップ軸から中心 (centerX/centerY) を除外、エッジ (left/right/top/bottom) のみに
- Shift 押下中はリサイズスナップをスキップ（縦横比固定モードでの干渉を防止）
- スナップ閾値を移動/リサイズで分離: 移動=10px、リサイズ=6px（緩め）
- size-match ガイドラインに寸法線風のエンドキャップを追加し、視認性向上

## [0.4.0] - 2026-04-07

### Added
- グリッドスナップ機能 (#22)
  - ツールバーにグリッドスナップ切替トグル追加（localStorage永続化）
  - 背景ドットとスナップグリッドを20px単位で統一
  - リサイズ時のスナップ対応（位置・サイズ両方を整列スナップ）
  - Figma風 size-match スナップ（同じ幅/高さのノードに吸着、寸法ラベル付き）
  - 等間隔スナップ（between/extend両対応、ガイドはアンカー中心に固定）
  - スナップガイドラインを再設計（オレンジ実線+ドットマーカー、寸法ラベル）
- Shift+リサイズで縦横比固定 (#14)
  - NodeResizer の `keepAspectRatio` を Shift キー押下中のみ有効化
  - 全ノードタイプ（NodeWrapper / ComponentInstanceNode / SubgraphGroupNode）に対応

### Changed
- ノードのデフォルトサイズを20pxグリッドの倍数に調整
  - 通常ノード: 150×50 → 160×60
  - hexagon: 170×60 → 180×60
  - cylinder: 150×70 → 160×80
  - predefinedProcess / display: 170×50 → 180×60
- React Flow の `selectionKeyCode` を `null` に設定し、Shift+ドラッグの範囲選択トリガーを無効化（リサイズ操作との競合を解消）

## [0.3.0] - 2026-04-01

### Added
- Mermaid importでmermaid.jsレンダリングによるレイアウト取得（Mermaid Live Editorと同等のノードサイズ・配置） (#26, #27)
- subgraphGroupノード: Mermaidの`subgraph`を視覚的グループとして取り込み（React Flow parentId親子関係）
  - ネストsubgraph対応（3階層以上）
  - 子ノードをキャンバス上で直接編集可能
  - subgraphGroup削除時に子ノード連動削除
- SubgraphGroupNodeコンポーネント（破線枠、ラベル、リサイズ、接続ハンドル）
- mermaid.jsレイアウトエンジン（`renderLayout.ts`）: SVGレンダリングから座標・サイズ抽出
- subgraph仕様書（`specs/subgraph.md`）

### Changed
- Mermaid出力を`graph`から`flowchart`構文に統一
- Mermaid importのsubgraph処理をComponentDefinition変換からsubgraphGroup方式に全面変更
- MermaidImportDialogをasync化（mermaid.jsレイアウト→autoLayoutフォールバック）

### Fixed
- Mermaid importのノードサイズ・配置がMermaid Live Editorと異なる問題 (#26)
- Mermaid importで文字数が多いノードから文字がはみ出す問題 (#27)
- Mermaid importで`-.->` (点線矢印)が認識されない問題
- Mermaid importでダブルクォート付きラベル(`"..."`)のクォートが残る問題
- Mermaid importで`<br/>`タグが文字列として残る問題（改行に変換）

## [0.2.2] - 2026-03-28

### Fixed
- BulkEditで改行対応 + ノード高さ自動拡張トグル (#31)
- BulkEditのズームフォーカスを改善（ビューポート内はスキップ）

## [0.2.1] - 2026-03-28

### Fixed
- 複数ノード選択時のまとめてサイズ変更に対応 (#23)
- テキストノードのゴースト表示を非表示に変更（メモ用途のため） (#28)
- stepエッジのダブルクリックをjog追加からラベル編集に変更 (#29)
- 改行ヒントをノードの外側（下部）に表示するよう変更 (#30)

### Changed
- 複数ノード選択時はゴースト（予測入力）を非表示に変更

## [0.2.0] - 2026-03-27

### Added
- 差分比較機能（サイドバイサイド方式）
  - 2ステップ画面構成: ファイル選択 → 差分比較表示
  - サイドバイサイドキャンバス（左=ベース、右=比較先、ビューポート同期）
  - 形状対応SVGオーバーレイ（15形状のグロー・内側影・枠線）
  - ADDED/DELETED/MODIFIEDバッジ表示
  - 差分検出エンジン（ノード全フィールド比較、エッジ追加/削除、コンポーネント定義）
  - Mermaidソースコード差分（統合/分割切替、LCSアルゴリズム）
  - フィルターチェックボックス（追加/削除/変更の強調表示切替）
  - フロー透過度スライダー（強調表示は影響なし）
  - テキスト差分クリック → キャンバス上のノード/エッジがアニメーション
  - ソースコード差分パネル: 折りたたみ可能（ヘッダー常時表示）

## [0.1.5] - 2026-03-22

### Added
- テキスト垂直配置（上/中央/下）をフォーマットバーに追加 (#9)
- テキスト編集時に「Shift+Enterで改行」ヒントを表示
- ノードの改行テキスト入力時に高さを自動拡張

### Fixed
- コンポーネント編集時のノードリサイズが定義に保存されない問題を修正

### Changed
- コンポーネント子ノードのフォントサイズをCHILD_SCALE(0.75)で縮小（編集画面と同じバランスで表示）

## [0.1.4] - 2026-03-22

### Fixed
- コンポーネントのLR方向が保存・復元されないバグを修正 (#1, #2)
- 折りたたみコンポーネントの選択時にフォーカス枠が表示されない問題を修正 (#5)
- コンポーネントリサイズ時に内部ノードのサイズが連動して変わる問題を修正 (#17)
- コンポーネントの左右ゴーストノード接続時に線が表示されない問題を修正 (#18)
- ブリッジエッジの接続先ハンドルが方向に応じて正しく設定されない問題を修正
- 折りたたみコンポーネントのSVG縦線がリサイズで変動する問題を修正

### Changed
- コンポーネントパネルにTD/LRラベル表示を追加
- ノード/コンポーネントタブにアイコンを追加
- 折りたたみコンポーネントのリサイズ対応（サイズ保持・復元）
- コンポーネント展開時にzIndexを自動引き上げ（子ノードが他ノードに隠れない）
- ゴーストノード表示をコンポーネントのフロー方向に制限（TD: 上下のみ、LR: 左右のみ）

## [0.1.3] - 2026-03-20

### Added
- エクスポートダイアログ新設（未配置コンポーネント定義を含めるトグル付き） (#21)

### Fixed
- 右クリックドラッグによるキャンバスパンが動作しない不具合を修正 (#24)
- 全ノード削除後もMermaidコードにエッジが残る不具合を修正 (#21)
- ゴーストノードへのエッジ接続がストアに保存される不具合を修正 (#7, #16)
- 最背面設定ノードが選択時に最前面に浮き上がる不具合を修正 (#8)
- 内部記憶ノードのリサイズ時に十字線の幅が変動する不具合を修正 (#19)
- 定義済み処理ノードのリサイズ時に縦線の幅が変動する不具合を修正 (#19)

### Changed
- OUTPUTパネルでキャンバスに未配置のコンポーネント定義を非表示に変更 (#21)

## [0.1.2] - 2026-03-19

### Changed
- ノード右クリックメニューの「複製」を「形状変更」サブメニューに置換（アイコン付き15種） (#6)

### Fixed
- コンテキストメニューから文字色をデフォルトに戻せないバグを修正 (#11)
- コンテキストメニューからエッジ線色をデフォルトに戻せないバグを修正 (#11)
- エッジ選択時・エッジ上での右クリックでコンテキストメニューが表示されないバグを修正 (#12)

## [0.1.1] - 2026-03-17

### Added
- β版ポップアップとGitHub Issuesボタン (#4)

### Fixed
- エッジの矢印（マーカー）が表示されない不具合を修正 (#10)
- エッジの線色変更時に矢印の先端のみ変わり線本体に反映されない不具合を修正 (#10)
- ゴーストエッジに矢印マーカーが付与されていない不具合を修正 (#10)
- BulkEdit Categoryモードのノード/エッジ表示順をキャンバス位置順に変更 (#3)

## [0.1.0] - 2026-03-16

Initial release.

### Added
- キャンバス上にノードを自由配置（ドラッグ&ドロップ）
- ノード形状 15種類
- リアルタイムMermaid記法生成
- 独自フォーマット（.flowmaid）のエクスポート/インポート
- Mermaid記法からの逆変換
- undo/redo（50履歴）
- コピー&ペースト
- コンポーネント（再利用可能テンプレート）
- テキスト一括編集（BulkEdit）モード
- 予測入力（ゴーストノード）
- ダーク/ライトテーマ
- 多言語対応（日本語/英語）
- ミニマップ
- 操作ガイドパネル
